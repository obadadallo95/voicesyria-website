# دليل إعداد الدومين - Domain Setup Guide

## 🎯 ربط الدومين `voicesyria.com` بالموقع

هذا الدليل يشرح خطوة بخطوة كيفية نشر الموقع وربطه بالدومين الخاص بك.

---

## 📋 الخطوة 1: إعداد الموقع للنشر

### 1.1 التحقق من الملفات المهمة

تأكد من وجود هذه الملفات:

- ✅ `package.json` - يحتوي على scripts للنشر
- ✅ `next.config.ts` - إعدادات Next.js
- ✅ `.env.local` - متغيرات البيئة (Supabase)
- ✅ `public/` - المجلد الذي يحتوي على الملفات الثابتة

### 1.2 بناء الموقع محلياً (اختياري للاختبار)

```bash
# في مجلد المشروع
cd voicesyria-website

# بناء الموقع
npm run build

# اختبار الموقع محلياً
npm run start
```

إذا كان الموقع يعمل على `http://localhost:3000` بدون أخطاء، فأنت جاهز للنشر!

---

## 🚀 الخطوة 2: نشر الموقع على Vercel (موصى به)

### 2.1 إنشاء حساب على Vercel

1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط على "Sign Up"
3. سجل دخولك باستخدام GitHub (موصى به) أو Email

### 2.2 رفع المشروع على GitHub (إذا لم يكن موجوداً)

#### أ. إنشاء مستودع جديد على GitHub

1. اذهب إلى [github.com](https://github.com)
2. اضغط على "New repository"
3. اسم المستودع: `voicesyria-website`
4. اختر "Private" (خاص) أو "Public" (عام)
5. **لا** تضيف README أو .gitignore (المشروع موجود بالفعل)
6. اضغط "Create repository"

#### ب. رفع المشروع إلى GitHub

```bash
# في مجلد المشروع
cd voicesyria-website

# تهيئة Git (إذا لم يكن موجوداً)
git init

# إضافة جميع الملفات
git add .

# عمل commit
git commit -m "Initial commit - Ready for deployment"

# إضافة مستودع GitHub (استبدل YOUR_USERNAME باسمك)
git remote add origin https://github.com/YOUR_USERNAME/voicesyria-website.git

# رفع المشروع
git branch -M main
git push -u origin main
```

### 2.3 نشر المشروع على Vercel

1. اذهب إلى [vercel.com/dashboard](https://vercel.com/dashboard)
2. اضغط على "Add New..." → "Project"
3. اختر المستودع `voicesyria-website` من GitHub
4. اضغط "Import"

#### إعدادات المشروع:

- **Framework Preset**: Next.js (يتم اكتشافه تلقائياً)
- **Root Directory**: `./` (افتراضي)
- **Build Command**: `npm run build` (افتراضي)
- **Output Directory**: `.next` (افتراضي)

#### إضافة متغيرات البيئة:

1. اضغط على "Environment Variables"
2. أضف المتغيرات التالية:

```
NEXT_PUBLIC_SUPABASE_URL=https://cljksetjseqnaragtlvb.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNsamtzZXRqc2VxbmFyYWd0bHZiIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTk3OTkzMjQsImV4cCI6MjA3NTM3NTMyNH0.cNu0az1z_xbragHobZmgihb1O10DeNMthdyDlu4hAa8
```

3. اضغط "Deploy"

### 2.4 الانتظار حتى اكتمال النشر

- Vercel سيبني الموقع تلقائياً
- سيستغرق الأمر 2-5 دقائق
- بعد اكتمال النشر، ستحصل على رابط مؤقت مثل: `voicesyria-website.vercel.app`

---

## 🌐 الخطوة 3: ربط الدومين

### 3.1 إضافة الدومين في Vercel

1. اذهب إلى مشروعك على Vercel
2. اضغط على "Settings" → "Domains"
3. أضف الدومين: `voicesyria.com`
4. أضف أيضاً: `www.voicesyria.com` (اختياري)

### 3.2 الحصول على DNS Records من Vercel

بعد إضافة الدومين، سيعطيك Vercel سجلات DNS التي تحتاجها:

مثال على السجلات:
```
Type: A
Name: @
Value: 76.76.21.21

Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

**احفظ هذه القيم!** ستحتاجها في الخطوة التالية.

---

## 🔧 الخطوة 4: إعداد DNS في GoDaddy

### 4.1 الدخول إلى GoDaddy Dashboard

1. اذهب إلى [dashboard.godaddy.com](https://dashboard.godaddy.com)
2. سجل دخولك بحسابك
3. في الصفحة الرئيسية، ستجد قسم "My Products"
4. ابحث عن الدومين `voicesyria.com` في القائمة
5. اضغط على الدومين (أو اضغط على "DNS" أو "Manage DNS" بجانبه)

### 4.2 تعديل سجلات DNS

#### أ. حذف السجلات القديمة (إن وجدت)

- احذف أي سجلات A أو CNAME قديمة

#### ب. إضافة سجلات جديدة

بعد إضافة الدومين في Vercel، سيعطيك Vercel قيم DNS محددة. استخدم هذه القيم:

**السجل الأول (A Record):**
1. اضغط على "Add" أو "Add Record"
2. اختر:
   - **Type**: `A`
   - **Name**: `@` (أو اتركه فارغاً)
   - **Value**: [انسخ القيمة من Vercel - مثال: `76.76.21.21`]
   - **TTL**: `600` (أو `Automatic`)
3. اضغط "Save"

**السجل الثاني (CNAME Record لـ www):**
1. اضغط على "Add" أو "Add Record"
2. اختر:
   - **Type**: `CNAME`
   - **Name**: `www`
   - **Value**: [انسخ القيمة من Vercel - مثال: `cname.vercel-dns.com`]
   - **TTL**: `600` (أو `Automatic`)
3. اضغط "Save"

**ملاحظة مهمة:**
- تأكد من حذف أي سجلات A أو CNAME قديمة للدومين
- قد تجد سجلات افتراضية مثل `@` بقيمة `parked-page.godaddy.com` - احذفها

#### ج. حفظ التغييرات

- اضغط "Save All Changes" أو "Save"
- قد يطلب منك تأكيد التغييرات
- التغييرات قد تستغرق من 5 دقائق إلى 48 ساعة (عادة 1-2 ساعة)

---

## ✅ الخطوة 5: التحقق من الربط

### 5.1 التحقق من DNS

استخدم أداة التحقق:

1. اذهب إلى [dnschecker.org](https://dnschecker.org)
2. أدخل `voicesyria.com`
3. ابحث عن نوع A
4. تأكد من أن القيم تطابق قيم Vercel

### 5.2 اختبار الموقع

بعد 1-2 ساعة من إعداد DNS:

1. افتح المتصفح
2. اذهب إلى `https://voicesyria.com`
3. يجب أن يفتح الموقع!

---

## 🔒 الخطوة 6: تفعيل HTTPS (SSL)

### 6.1 تفعيل SSL في Vercel

1. Vercel يفعل SSL تلقائياً!
2. اذهب إلى "Settings" → "Domains"
3. تأكد من أن SSL Certificate موجود و "Valid"

### 6.2 إجبار HTTPS (اختياري)

Vercel يوجه HTTP إلى HTTPS تلقائياً، لكن يمكنك التأكد:

1. اذهب إلى `next.config.ts`
2. تأكد من أن الإعدادات صحيحة

---

## 📝 الخطوة 7: الإعدادات النهائية

### 7.1 تحديث Metadata في الموقع

تأكد من أن `app/layout.tsx` يحتوي على:

```typescript
export const metadata: Metadata = {
  title: "صوت سوريا - Syria Voice",
  description: "...",
  // ... إلخ
};
```

### 7.2 إضافة ملف APK

1. ضع ملف APK في `public/apk/app.apk`
2. تأكد من أن رابط التحميل يعمل: `/apk/app.apk`

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: الدومين لا يعمل

**الحل:**
- انتظر 1-2 ساعة (DNS يحتاج وقت للانتشار)
- تأكد من أن سجلات DNS صحيحة
- استخدم [dnschecker.org](https://dnschecker.org) للتحقق

### المشكلة 2: خطأ SSL

**الحل:**
- Vercel يفعل SSL تلقائياً
- انتظر 5-10 دقائق بعد إضافة الدومين
- تأكد من أن الدومين مربوط بشكل صحيح

### المشكلة 3: الموقع لا يظهر

**الحل:**
- تأكد من أن النشر على Vercel نجح
- تحقق من سجلات DNS
- امسح الكاش من المتصفح (Ctrl + Shift + Delete)

---

## 📞 الدعم

إذا واجهت مشاكل:

1. تحقق من [Vercel Documentation](https://vercel.com/docs)
2. تحقق من [GoDaddy Help](https://www.godaddy.com/help)
3. راجع أخطاء Console في المتصفح (F12)

---

## ✅ قائمة التحقق النهائية

- [ ] الموقع يعمل محلياً (`npm run build` و `npm run start`)
- [ ] المشروع على GitHub
- [ ] المشروع منشور على Vercel
- [ ] متغيرات البيئة مضافة في Vercel
- [ ] الدومين مضاف في Vercel
- [ ] سجلات DNS محدثة في GoDaddy
- [ ] انتظرت 1-2 ساعة للانتشار
- [ ] الموقع يعمل على `https://voicesyria.com`
- [ ] SSL Certificate مفعل

---

**مبروك! موقعك الآن متاح على `https://voicesyria.com` 🎉**

